rootProject.name = "EposPosLinkJvmExample"

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        // androidx.* (annotation, collection, compose runtime) is not on Maven Central.
        google()
    }
}
